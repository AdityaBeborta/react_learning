const LazyComp = () => {
  return (
    <>
      <div>Lazy Component Demo</div>
    </>
  );
};
//here we need to use export default only, we cannot use export const LazyComp =()=>{}
export default LazyComp;
